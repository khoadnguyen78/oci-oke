apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "example-typescript-package.fullname" . }}-test-connection"
  labels:
    {{- include "example-typescript-package.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "example-typescript-package.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
