apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "example-typescript-package.fullname" . }}
  namespace: {{ .Release.Namespace }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "example-typescript-package.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "example-typescript-package.name" . }}
    spec:
      imagePullSecrets:
        {{- toYaml .Values.imagePullSecrets | nindent 8 }}
      containers:
        - name: {{ include "example-typescript-package.name" . }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: {{ .Values.service.port }}
