apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "docker-nodejs-application.fullname" . }}-test-connection"
  labels:
    {{- include "docker-nodejs-application.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "docker-nodejs-application.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
